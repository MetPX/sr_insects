
broker amqp://tsource@${FLOWBROKER}

exchange xs_tsource_shim

queue_name q_${BROKER_USER}_${COMPONENT}_${CONFIG}_${HOSTNAME}

topic_prefix v03.post

subtopic #

mirror off

preserve_time off

topic_prefix v03.post

report_back off


directory ${TESTDOCROOT}/mirror
accept_unmatch on
